---
// Newsletter.astro
interface Props {
  tipo?: 'blog' | 'servicios';
}

const { tipo = 'blog' } = Astro.props;
---

<section class="bg-[#0e3146]/50 border border-white/10 rounded-3xl p-8 md:p-12 my-20 relative overflow-hidden shadow-2xl">
  <div class="absolute top-0 right-0 p-4 opacity-10">
    <svg width="60" height="60" viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg">
      <circle cx="50" cy="50" r="40" stroke="white" stroke-width="0.5" stroke-dasharray="4 4"/>
      <path d="M50 10V90M10 50H90" stroke="white" stroke-width="0.5"/>
    </svg>
  </div>

  <div class="max-w-2xl mx-auto text-center relative z-10">
    <h2 class="text-3xl md:text-4xl font-['Orbitron'] font-black text-white mb-4 uppercase tracking-tight">
      {tipo === 'blog' ? (
        <>
          RECIBE DE <span class="text-[#e9691e]">BITS</span> A <span class="text-[#2a99c9]">BOTS</span>
        </>
      ) : (
        /* En servicios lo dejamos en blanco (blanco puro) para que destaque el mensaje de negocio */
        "ESTRATEGIA EN TU RADAR"
      )}
    </h2>
    
    <p class="text-slate-400 font-['Inter'] mb-10 leading-relaxed max-w-lg mx-auto">
      {tipo === 'blog' 
        ? 'Únete a la red de líderes técnicos. Envío lecciones directas sobre sistemas legacy e IA.' 
        : 'Recibe insights estratégicos sobre automatización y modernización para mantener tu ventaja competitiva.'}
    </p>
    
    <div class="max-w-md mx-auto">
      <form action="https://e921d5a3.sibforms.com/serve/..." method="POST" class="space-y-4">
        <input 
          type="email" 
          name="EMAIL" 
          placeholder="protocolo@correo.com" 
          required
          class="w-full px-5 py-4 rounded-xl bg-[#0a1e2b] border border-white/10 text-white font-['Fira+Code'] text-sm focus:ring-2 focus:ring-[#2a99c9] outline-none transition-all"
        />
        
        <button 
          type="submit"
          class="w-full px-8 py-4 bg-[#e9691e] hover:bg-[#d8581a] text-white font-['Orbitron'] text-xs font-bold uppercase tracking-[0.2em] rounded-xl transition-all shadow-lg shadow-[#e9691e]/20"
        >
          {tipo === 'blog' ? 'Sincronizar Suscripción' : 'Activar Radar'}
        </button>
      </form>
    </div>
    
    <p class="text-[10px] font-['Fira+Code'] text-slate-500 mt-8 uppercase tracking-widest opacity-60">
      0% Spam / 100% Technical Value
    </p>
  </div>
</section>